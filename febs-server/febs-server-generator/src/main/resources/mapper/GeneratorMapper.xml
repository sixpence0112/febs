<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cxf.febs.sever.generator.mapper.GeneratorMapper">

    <!--查询可用数据库-->
    <select id="getDatabases" resultType="java.lang.String">
        select distinct table_schema from information_schema.tables
    </select>

    <!--查询数据库下面的表-->
    <select id="getTables" resultType="com.cxf.febs.common.core.entity.system.Table" parameterType="string">
        select
        create_time createTime,
        update_time updateTime,
        table_rows dataRows,
        table_name name,
        table_comment remark
        from
        information_schema.tables
        where
        table_schema = #{schemaName}
        <if test="tableName != null and tableName != ''">
            and table_name = #{tableName}
        </if>
    </select>

    <!--查询数据库表下面的列属性-->
    <select id="getColumns" resultType="com.cxf.febs.common.core.entity.system.Column">
        select
        column_name name,
        case
            column_key
        when 'PRI' then
                1 else 0
        end isKey,
        data_type type,
        column_comment remark
        from information_schema.columns
        where table_schema = #{schemaName} and table_name = #{tableName}
    </select>
</mapper>
